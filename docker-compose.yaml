services:
  app:
    build: .
    container_name: fahadpervej-mditems-app
    restart: unless-stopped
    ports:
      - "3000:3000"
    environment:
      - NODE_ENV=production
      - MONGODB_URI=mongodb+srv://fahad_db_user:WEejxtGLxZtuoSeS@cluster0.mongodb.net/myapp
    networks:
      - app-network

networks:
  app-network:
    driver: bridge